<%= render 'sidebar' %>

<h1>Your Templates</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th> Template Name </th>
      <th></th>
      <th> Signature Position</th>
      <th colspan="3"></th>
      
    </tr>
  </thead>

  <tbody>
    <% @templates.each do |template| %>
      <tr class="<%= template.processed_attachement_url ? 'success' : 'danger' %>">
        <td><%= template.name %></td>
        <% unless template.processed_attachement_url.blank? %>
        <td><%= link_to 'View', template %></td>
        <td>
          <% if template.signature_position.blank? %><%= link_to 'Set Signature Position', edit_template_path(template) %></td> <% else %> <%= image_tag('checkmark.png', :class => 'center')  %>  Already Set<% end %>
        <td><%= link_to 'Delete Template', template, method: :delete, data: { confirm: "Are you sure?  Any documents you've created using this template will no longer be available" } %></td>
<% else %>
        <td> </td>
        <td> </td>
        <td> </td>
        <td> </td>
        <% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button_to 'Create a new Template', new_template_path, :class =>"btn btn-success", :method => :get  %>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-content-wrapper -->

    </div>
    <!-- /#wrapper -->

    <!-- jQuery -->
    <script src="/javascripts/jquery.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="/bootstrap.min.js"></script>

    <!-- Menu Toggle Script -->
    <script>
    $("#menu-toggle").click(function(e) {
        e.preventDefault();
        $("#wrapper").toggleClass("toggled");
    });
    </script>

</body>

</html>